# 内容生成输出项目 - 项目执行清单

更新时间：2026-03-24
状态口径：Todo / In Progress / Review / Done / Dropped

---

## 一、总目标

将项目收口为：

- WebApp 主操作面
- 9阶段串行工作流主路径
- CLI 完整并行通道
- Shared Core 单一执行逻辑
- 可人工控制、可追溯、低维护

---

## 二、P0 架构定板（优先）

1. 定版主流程说明
- 状态：Todo
- 交付：统一 9 阶段说明与操作入口图

2. 定版三分区产品结构（首页/工作流/工具箱）
- 状态：Todo
- 交付：界面信息架构与入口边界

3. 定版双通道能力对齐
- 状态：In Progress
- 进展：已共享任务状态、阶段定义、确认机制、回退能力与 run-range 状态快照；前端仍有旧 5 步分组视图遗留

4. 明确当前阶段不接入 cron
- 状态：Done
- 结论：结构稳定后再接入

5. 目录结构角色梳理
- 状态：In Progress
- 进展：文档已合并收口；后续配合代码目录再推进物理迁移

---

## 三、P1 工程收口

6. 抽离共享 pipeline / service 层
- 状态：In Progress
- 目标：WebApp 与 CLI 只保留入口差异，业务逻辑共用
- 最新进展：`hotspot-list / hotspot-select / hotspot-enrich` 已切到 shared step executor；draft / platform / review / compose / export 仍有 Web 路由遗留实现

7. CLI Runner 完整化
- 状态：In Progress
- 已有：任务创建、阶段推进、区间执行、重试、恢复、待确认阶段阻断、显式回退
- 待补：补齐帮助文档、统一错误文案、继续减少对 `webapp/services` 的直接依赖

8. 配置与环境变量统一
- 状态：Todo
- 目标：文档、代码、`.env.example` 三方一致

9. 清理旧脚本与重复实现
- 状态：Todo
- 重点：`tools/publish` / `tools/scheduler` / `tools/scraper` 与旧入口

---

## 四、P2 体验收口

10. 重做工作流界面
- 状态：In Progress
- 进展：已确定改为“热点独立 + 工作流 6 步展示”，共享执行核心仍保留 9 阶段

11. 阶段承接 + 人工确认机制
- 状态：In Progress
- 进展：需要确认的阶段已不再默认自动放行；任务摘要卡已支持确认下一阶段、回退到指定阶段、回退并重跑

12. 热点中心改造（以 Google Sheets 为主）
- 状态：In Progress
- 进展：已打通 list/select/enrich 最小闭环，Web 与 CLI 共用状态

13. 文本资产体系建设
- 状态：Todo
- 分类：写作策略 / 平台适配 / 去AI规则 / 审核规则 / 图片提示词

14. 视觉输出链路统一
- 状态：Todo

15. 导出与输出规范统一
- 状态：Todo

---

## 五、P3 文档收口

16. 入口图文档
- 状态：Todo

17. 自动化接入说明
- 状态：Todo

18. 目录结构说明
- 状态：Todo

19. 主流程操作说明
- 状态：Todo

---

## 六、执行顺序建议

1. 先规则：主流程、产品结构、双通道口径、目录角色
2. 再底层：共享 core、CLI runner、配置统一、旧脚本清理
3. 再体验：工作流界面、阶段承接、热点中心、导出规范
4. 最后文档：入口图、接入说明、操作说明

---

## 七、最近落地（2026-03-24）

1. 修正任务确认语义
- `run-step` / `run-range` 不再默认越过需要人工确认的阶段。

2. 增加任务回退能力
- 已支持回退到指定阶段，并裁剪 `completedStages`、清空 `pendingConfirmationStage`。

3. 统一批量执行状态快照
- Web 与 CLI 都会把 `runRange` 结果写回任务元数据，用于续跑与界面摘要展示。

4. 开始收口 Web/CLI 双实现
- 热点三阶段已切到共享执行器，后续继续收口 draft / platform / review / compose / export。

---

## 八、关联专题

- 主线方案：`项目主线方案.md`
- 反AI专题：`反AI体系重构合并稿.md`
