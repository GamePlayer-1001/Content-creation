# webapp/public/js/components/
> L2 | 父级: /webapp/public/js/CLAUDE.md

成员清单
editor.js: 文本编辑器组件，承载 Markdown/YAML 编辑体验。
modal.js: 模态框组件，负责弹窗开关与内容承载。
nav.js: 侧边导航组件，负责页面入口展示。
stream.js: SSE 流式渲染组件，负责把生成流写入界面。
toast.js: 全局通知组件，负责反馈提示。

法则: 组件做可复用交互原子·不承载跨页面业务规则

[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
